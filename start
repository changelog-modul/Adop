

--// LOAD WIND UI
local WindUI = loadstring(game:HttpGet(
    "https://github.com/Footagesus/WindUI/releases/latest/download/main.lua"
))()

--// CONFIRM STATE
local Confirmed = false

--// POPUP
WindUI:Popup({
    Title = "PHOENIXX-HUB",
    Icon = "cuboid",
    IconThemed = true,
    Content =
    "⚠️ Warning\n\n" ..
    "To avoid bugs or errors, please disable all Delta / Executor settings before executing this script.",
Buttons = {
    {

            Title = "Cancel",
            Variant = "Secondary",
            Callback = function()
                Confirmed = false
            end
        },
        {
            Title = "Continue",
            Icon = "arrow-right",
            Variant = "Primary",
            Callback = function()
                Confirmed = true
            end
        }
    }
})

--// WAIT USER INPUT
repeat task.wait() until Confirmed ~= false

--// STOP IF CANCEL
if not Confirmed then
    return
end

-- =====================================
-- SCRIPT LANJUT DI BAWAH INI
-- =====================================
loadstring(game:HttpGet("https://raw.githubusercontent.com/changelog-modul/Adop/refs/heads/main/visual", true))()

print("Confirmed. Script berjalan...")




